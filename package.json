{
  "name": "dollar-commander",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dollar Commander browser extension",
  "scripts": {
    "build": "node build.js",
    "build:chrome": "node build.js chrome",
    "build:firefox": "node build.js firefox",
    "watch": "node build.js --watch",
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "web-ext": "^10.1.0"
  }
}

